a = new Array( "beta", "gamma","alpha", "delta")
b = new Array("a", "b", "c", "d")
matrix = new Array(a, b)
for (i = 0; i < a.length; i++) {
          for (j = 0; j < a[i].length; j++) {
          // console.log(a[i][j]) 
          }
}

//let join = matrix.join('');
let sort = matrix.reverse().join('');
//let reverse = matrix.

//console.log(join)
console.log(sort)
//console.log(reverse)